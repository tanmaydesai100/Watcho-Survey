# Optional: use these when you add env-based config (e.g. in Vite with VITE_ prefix).
# Copy to .env.local and fill in for local development.

# Google Apps Script web app URL (POST endpoint for survey submissions)
# VITE_SCRIPT_URL=https://script.google.com/macros/s/.../exec

# Redirect URL when user closes the success popup
# VITE_WATCHO_WEBSITE=https://www.watcho.co.uk
